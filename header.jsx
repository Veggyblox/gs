function Header() {
  return (
    <header className="hero">
      <div>
        <p className="eyebrow">AI + Open Library explorer</p>
        <h1>AI Book Explorer</h1>
        <p className="hero-text">
          Choose a mood and genre, let AI suggest books, then explore real book cards, fast details, and a smooth scrollable recommendation strip.
        </p>
      </div>
    </header>
  );
}

export default Header;
